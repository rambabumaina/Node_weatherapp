//All important commands and point here 

/*
HEROKU:

1) Setup SSH public key file with heroku
heroku keys:add

2) Create Heroku application name using below command
Heroku create ram-stock-weather-app

3) Make package.json as starting pint
  "scripts": {
    "start": "node src/app.js"
  }

4) Set port for the application which is running


4) To push application on heroku and run. Application gety pulled codebase from Git
git push heroku master

_____________________
Node Commnds:
1)npm uninstall -g nodemon : To unistall package globaly in your machine
2)npm uninstall -g nodemon : To install Package galobaly
3)npm install nodemon@2.1.2 --save-dev : To install Package within the project


------------------------
MonogDB Start Command:
"C:\m-db\mongodb\bin\mongod.exe" --dbpath="C:\m-db\mongodb-data"
 npm install mongodb --save
 npm install mongoose

*/
